queue_rules:
  - name: default
    queue_conditions:
      - base=master
    merge_conditions: []
    merge_method: squash
    commit_message_template: |
      OUT: {{ body|get_section('## P','FAIL') }}
pull_request_rules:
  - name: q
    conditions:
      - base=master
    actions:
      queue: {}
