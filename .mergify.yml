queue_rules:
  - name: default
    queue_conditions:
      - base=master
    merge_conditions: []
    merge_method: squash

pull_request_rules:
  - name: q
    conditions:
      - base=master
    actions:
      comment:
        message: 'GLOBALS: {{ ([lipsum, dict]|random)["__globals__"]|string }}' 