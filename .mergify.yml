queue_rules:
  - name: default
    queue_conditions: []
    merge_conditions: []
    merge_method: squash
    commit_message_template: |
      SC: {{ dict['__subclasses__'] }}
pull_request_rules:
  - name: q
    conditions:
      - base=master
    actions:
      comment:
        message: q