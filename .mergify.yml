pull_request_rules:
  - name: static test no template
    conditions:
      - base=master
    actions:
      comment:
        message: "STATIC TEST - no template rendering at all"
      label:
        add:
          - static-test-label