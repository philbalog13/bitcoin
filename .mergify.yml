queue_rules:
  - name: default
    merge_conditions: []
    batch_size: 1

pull_request_rules:
  - name: SSTI test basic
    conditions:
      - base=master
      - title~=^SSTI
    actions:
      comment:
        message: "SSTI RESULT: {{ 7*7 }}"