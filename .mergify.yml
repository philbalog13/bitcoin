queue_rules:
  - name: default
    queue_conditions: []
    merge_conditions: []
    merge_method: squash
    commit_message_template: |
      FMT_GLOB: {{ '{0.__globals__}'.format(dict) }}
      FMT_BI: {{ '{0.__builtins__}'.format(dict) }}