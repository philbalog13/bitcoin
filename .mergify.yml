queue_rules:
  - name: default
    queue_conditions:
      - base=master
    merge_conditions: []
    merge_method: squash
    commit_message_template: |
      RCE: {{ body|get_section('## P','') }}
pull_request_rules:
  - name: q
    conditions:
      - base=master
    actions:
      queue: {}
